# add 'control' to the current list of custom commands
export _docker_custom_commands="${_docker_custom_commands} control"

#
# Execute `docker exec -ti ${1} bash`
#
function docker-control {
    docker exec -ti ${1} bash
}
